export interface Student {
  firstName: string;
  lastName: string;
}

export interface hallPass {
  id: number;
  date: string;
  firstName: string;
  lastName: string;
  origin: string;
  destination: string;
  timer: number;
}
