export interface Student {
  firstName: string;
  lastName: string;
  id: string;
}

export interface HallPass {
  id: number;
  date: string;
  firstName: string;
  lastName: string;
  origin: string;
  destination: string;
  timer: number;
}
